# 项目清理设计文档

> **日期**: 2026-02-13
> **状态**: 已批准
> **方案**: 简洁清理(方案 B)

---

## 1. 清理目标

整理项目代码,移除过时的文档和文件,优化 .gitignore 配置,保持项目结构清晰简洁。

## 2. 清理范围

### 2.1 设计文档清理

**删除的文档:**
- `docs/plans/2026-01-29-meditation-website-design.md` - 初始网站设计文档(已被新设计取代)
- `docs/plans/2026-02-09-mindful-prd.md` - 产品需求文档(已被社区功能设计取代)

**保留的文档:**
- `docs/plans/2026-02-13-meditation-community-design.md` - 最新社区功能设计
- `docs/plans/2026-02-13-meditation-community-implementation.md` - 最新实现计划

**理由:** 旧文档的内容已经被新的社区功能设计整合和更新,保留旧版本会造成混淆。Git 历史中仍然保留了这些文件,需要时可以查看。

### 2.2 设计文件整理

**创建目录:**
- `docs/design/` - 统一存放设计相关文件

**移动文件:**
- `DESIGN_SYSTEM.md` → `docs/design/DESIGN_SYSTEM.md`
- `design-preview.html` → `docs/design/design-preview.html`

**理由:** 这些设计文件应该与文档放在一起,保持项目根目录整洁。

### 2.3 .gitignore 优化

**添加内容:**

```gitignore
# Environment variables
.env
.env.local
.env.development
.env.test
.env.production
.env*.local

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
```

**理由:**
- 环境变量文件可能包含敏感信息(API keys, 数据库密码等),必须忽略
- OS 文件是系统自动生成的,不应纳入版本控制
- 这些是业界标准做法

## 3. 清理后的目录结构

```
ccmeditation/
├── .gitignore (更新)
├── docs/
│   ├── design/ (新建)
│   │   ├── DESIGN_SYSTEM.md (从根目录移动)
│   │   └── design-preview.html (从根目录移动)
│   └── plans/
│       ├── 2026-02-13-meditation-community-design.md (保留)
│       ├── 2026-02-13-meditation-community-implementation.md (保留)
│       └── 2026-02-13-project-cleanup-design.md (本文档)
├── meditation-website/
├── server/
└── ...
```

## 4. 执行步骤

1. 创建 `docs/design/` 目录
2. 移动 `DESIGN_SYSTEM.md` 到 `docs/design/`
3. 移动 `design-preview.html` 到 `docs/design/`
4. 删除 `docs/plans/2026-01-29-meditation-website-design.md`
5. 删除 `docs/plans/2026-02-09-mindful-prd.md`
6. 更新 `.gitignore` 文件
7. 提交所有更改
8. 推送到远程仓库

## 5. 风险评估

**风险:** 删除的文档可能包含有用信息

**缓解措施:**
- Git 历史完整保留所有删除的文件
- 可以通过 `git log` 和 `git show` 查看历史版本
- 如需恢复,可以使用 `git checkout <commit> -- <file>` 恢复

**风险:** 移动文件可能导致引用失效

**缓解措施:**
- 项目中没有代码引用这些 Markdown 和 HTML 文件
- 这些是文档文件,不影响代码运行

## 6. 清理清单

- [x] 探索项目结构
- [x] 确认清理范围
- [x] 设计清理方案
- [x] 编写设计文档
- [ ] 执行文件操作
- [ ] 提交更改
- [ ] 推送到远程

---

**设计完成,准备执行!** 🚀
